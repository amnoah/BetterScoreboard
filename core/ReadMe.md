# BetterScoreboard-core

This module includes the bulk majority of the logic for BetterScoreboard. This module is not based on any specific
Minecraft platform, but rather just PacketEvents. It can run on any platform where PacketEvents exists (as long as the
appropriate bridges are provided), making it easy to implement the plugin on any platform.